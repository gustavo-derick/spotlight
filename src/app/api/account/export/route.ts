import { NextResponse } from 'next/server'

// GET /api/account/export — implementado no Bloco 9
export async function GET() {
  return NextResponse.json({ error: 'Não implementado' }, { status: 501 })
}
